#!/usr/bin/env bash

BUILD_ARGS=''

if [[ "$1" == '--force' ]]; then
    BUILD_ARGS="$BUILD_ARGS --no-cache"
fi

docker-compose -f docker-compose.yml -f $BACKEND_DIR/docker-compose.yml build $BUILD_ARGS